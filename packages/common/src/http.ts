export enum HttpMethod {
  Get = "get",
  Post = "post",
  Put = "put",
  Patch = "patch",
  Delete = "delete",
}

export enum HttpStatusCode {
  Success = 200,
}

export enum HttpStatusCode {
  NotFound = 404,
}

export enum HttpStatusCode {
  ServerError = 500,
}
